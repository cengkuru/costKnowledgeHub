export const environment = {
  production: false,
  // Use local API for faster development
  apiBaseUrl: 'http://localhost:3000/api',
  // Alias for backwards compatibility with existing services
  apiUrl: 'http://localhost:3000/api',
};
